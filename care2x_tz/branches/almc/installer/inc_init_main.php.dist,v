head	1.1;
access;
symbols
	care2watu:1.1.0.4
	Root_care2watu:1.1
	dodoma:1.1.0.2
	Root_dodoma:1.1;
locks; strict;
comment	@# @;
expand	@b@;


1.1
date	2008.04.01.12.57.20;	author marcel;	state Exp;
branches;
next	;


desc
@@


1.1
log
@add installer
fixed problem with create user
fixed nursing station view
@
text
@<?php
# This is the database name
$dbname='INSTALL_DB_DATABASE';

# Database user name, default is root or httpd for mysql, or postgres for postgresql
$dbusername='INSTALL_DB_USERNAME';

# Database user password, default is empty char
$dbpassword='INSTALL_DB_PASSWORD';

# Database host name, default = localhost
$dbhost='INSTALL_DB_SERVER';

# First key used for simple chaining protection of scripts
$key='INSTALL_KEY_1';

# Second key used for accessing modules
$key_2level='INSTALL_KEY_2';

# 3rd key for encrypting cookie information
$key_login='INSTALL_KEY_3';

# Main host address or domain
$main_domain='INSTALL_ADDRESS';

# Host address for images
$fotoserver_ip='INSTALL_ADDRESS';

# Transfer protocol. Use https if this runs on SSL server
$httprotocol='INSTALL_PROTOCOL';

# Set this to your database type. For details refer to ADODB manual or goto http://php.weblogs.com/ADODB/
$dbtype='INSTALL_DB_TYPE';
?>@
