<?php if (!defined('INSTALLER_PATH')) { header('Location: index.php'); exit; } ?>

At this point we are ready to confirm the database connection and check
for an existing installation.<br />
<br />
If we want to allow the option to patch up the data (ie upgrade rather
than install), here is where we would do it.
